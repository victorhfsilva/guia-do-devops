# Clone

Para clonar um repositório:
```
git clone <endereço_ssh_repositório>
```

Para especificar o nome da pasta em que o repositório será clonado:

```
git clone <endereço_ssh_repositório> <nome da pasta>
```